# 2026-04-09

- Desire Cache prototype priority was reset away from aesthetics and back to fundamentals: all items reachable, image visibility restored, navigation obvious, and movement through the staged map no longer trap-prone.
- The graph-native traversal pass was simplified rather than further embellished. Tree Mode now relies on native scrolling through a large map workspace, with section chips that jump to neighborhoods and subsection chips that jump to branch clusters instead of hiding most content behind one active branch.
- All subsection clusters for open neighborhoods are now rendered in the staged build, not just a single active branch. Validation against the public staged HTML counted `67` rendered item-card visual blocks, matching the `67` items in the canonical `/api/items` payload.
- Live payload sanity check showed all `67` current items include non-empty image URLs. The frontend now also treats image backing as first-class by rendering either the live image or an explicit fallback tile per item card if an image fails.
- The operability-focused pass passed `npm run check` and `npm run build`, was redeployed to `http://10.135.30.250/dc-next/`, and the new CSS asset bundle returned HTTP 200 after deployment.
- Desire Cache then received a stronger graph-first / identity / taxonomy pass. Tree Mode now renders compact item tokens first and uses a detail dock for hover/tap expansion instead of leading with full item cards. Section presentation identity is now themed (`Perk Injector`, `Comfort Daemon`, `Shell Mods`, `Rig Matrix`) rather than a direct dump of raw sheet labels, while branch grouping derives from the data and falls back to source/vendor labels when a branch label is weak.
- The new graph-first pass passed `npm run check` and `npm run build`, was redeployed to staging, and validation against the public staged HTML confirmed `67` actual rendered node tokens, matching the `67` items in `/api/items`.
- Desire Cache then received a composition/flow refinement on top of that foundation. The section placements are now less symmetrically orbital, branch lanes curve and fan more organically, tokens are staggered instead of marching in a hard micro-grid, and neighborhood shells were softened so the board feels less like stacked cards on a strict board.
- The composition pass still preserved operability and the graph-first machine, passed `npm run check` and `npm run build`, was redeployed to staging, and the public staged HTML still rendered `67` actual node tokens with the new asset bundle returning HTTP 200.
- Desire Cache then received a stronger free-space correction aimed specifically at breaking remaining neighborhood/container logic. Section shells were visually dissolved further, branch and detail space now occupy a more shared field, and the board leans harder toward floating game-space rather than obvious nested panels.
- The freer-space pass still passed `npm run check` and `npm run build`, was redeployed to staging, and the public staged HTML continued to render `67` actual node tokens with the new CSS asset returning HTTP 200.
- Desire Cache staging at http://10.135.30.250/dc-next/ remains functional and safe, but Zoat’s blunt product verdict after multiple Geld passes is that it still feels boxed, messy, and not truly like a flowing skill-tree / game-shop / circuit-web.
- Multiple iterative UI passes were completed by Geld on the SvelteKit app: multi-open neighborhoods, clearer controls, image restoration, graph-first node field, themed section naming, looser/freer composition, and reduced visible shells. Despite these improvements, Zoat still judged the browsing model as fundamentally too boxed and not matching the desired references.
- Strong updated product direction from Zoat: the target interaction is closer to Cyberpunk / ARC Raiders style skill trees, with compact nodes first, hover on desktop / tap on mobile to reveal richer detail, easier relation tracing, and a more playful videogame-like browsing feel.
- Zoat explicitly proposed a possible architectural pivot: consider using React plus specialized open-source skill-tree/node-graph libraries (specifically Beautiful Skill Tree, React Flow, or possibly SkillTree Platform) if that is what it takes to get the interaction/layout in the right direction.
- If work resumes after compaction, the most important open decision is whether to keep forcing the current SvelteKit implementation toward the desired feel, or pivot the front-end approach/library to something better suited for true skill-tree / graph-native interaction.
# 2026-04-09

- Desire Cache frontend direction effectively pivoted from forcing the SvelteKit graph experiment toward a React-based approach after Zoat reaffirmed preference for a Beautiful Skill Tree style / React path and approved proceeding.
- A separate React prototype app was created in `react-pivot/` to avoid disturbing the existing staged SvelteKit lane. It uses the canonical `/api/items` feed, React Flow as the active graph engine, and keeps Beautiful Skill Tree installed as a comparison/reference library rather than the primary renderer.
- Early React pivot milestones completed: production build passes, custom section/branch/item node renderer added, hover/click-driven dossier panel added, and the graph styling was pushed toward a more game-like lattice rather than a raw diagramming demo.
- The React pivot was staged separately on the desire-cache host at `/dc-react/` while leaving `/dc-next/` untouched. Initial blank-screen failure was caused by Vite outputting root-relative asset paths instead of `/dc-react/`-scoped asset URLs; this was fixed by setting the Vite base path for staging.
- A second staging/runtime issue then surfaced: browser-visible fetches initially targeted `127.0.0.1:5000`, causing `Failed to fetch` in the client. The production build was corrected to use relative `/api/items` requests for the browser-facing staged path.
- Zoat reviewed the React graph staging and judged the direction more promising than the old SvelteKit approach, but still too cramped, insufficiently section-organized, and visually weak at showing ownership/connection despite the presence of link lines.
- In response, the React prototype received a layout pass that introduced explicit section neighborhood zones, clearer branch rails, more compact item nodes, and reduced item-connector noise, improving legibility but still not fully matching the desired aesthetic.
- Critical updated product direction from Zoat after reviewing fresh references: the stronger target is not a full always-on skill-tree board, but an ARC Raiders-like inventory/shop interface with dense readable item grids, category framing, strong persistent dossier/detail panel, and only subtle skill-tree/relationship DNA rather than graph clutter everywhere.
- Current best recommendation after the latest review is to pivot `/dc-react/` again, this time toward a stylized game-shop / inventory shell as the main browsing model, keeping relation traces only as a secondary accent or focused-state behavior rather than the core layout.

## Ollama Cloud + SearXNG Infrastructure

- Deployed dedicated SearXNG instance in PVE CT 100 (hostname: searxng, IP: 10.135.30.251:8080)
  - Debian 13 LXC, Docker-based deployment from searxng/searxng repo
  - Two containers: searxng-core + searxng-valkey
  - Config: `/opt/searxng/core-config/settings.yml`
  - Fixed YAML formatting bug in `formats:` block (html+json), disabled limiter for LAN access
  - Working and serving both HTML and JSON API responses

- Ollama cloud models provisioned via `ollama run <model>:cloud`:
  - `glm-5.1:cloud` — primary reasoning/general intelligence (Great Sage)
  - `qwen3-coder-next:cloud` — coding specialist
  - `gemma4:31b-cloud` — lightweight/third-opinion tasks

- OpenClaw config now properly aligned:
  - Primary: `ollama/glm-5.1:cloud` (203k context)
  - Fallbacks: `ollama/qwen3-coder-next:cloud` → `openai-codex/gpt-5.4`
  - Ollama auth: device-bound via `ollama signin`, `api_key` mode at OpenClaw layer
  - Removed stale models (kimi-k2.5, minimax-m2.5, glm-5:cloud) that weren't actually provisioned
  - Removed incorrect `apiKey: "OLLAMA_API_KEY"` placeholder

- SearXNG wired as OpenClaw web search provider:
  - Config: `plugins.entries.searxng.config.webSearch.baseUrl = http://10.135.30.251:8080`
  - `tools.web.search.provider = searxng`
  - Verified working: `web_search` tool returns results through the self-hosted instance

- Key lesson: the `ollama launch openclaw` flow is a local integration configurer, not a cloud-hosted second OpenClaw. But for an existing installation, `openclaw configure` is sufficient.

- Ollama cloud model provisioning requires `ollama signin` first, then `ollama run <model>:cloud` to make models available. Without signin, `ollama list` shows empty and cloud models return 401.
