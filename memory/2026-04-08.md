# 2026-04-08

- Mattermost tunnel issue was diagnosed and effectively fixed. CT 1111 (`cloudflared`) had been running the wrong tunnel config (`home-rdp`), while Mattermost ingress belonged to tunnel `d2c361c2-20f3-40a9-afd0-c7cf360bd19e`. Server-side config was corrected so CT 1111 now launches the Mattermost tunnel config, and `https://mattermost.aualaohana.com/api/v4/system/ping` returned HTTP 200 afterward.
- Cloudflare-side confusion briefly manifested as 404 then 1033 while `mattermost.aualaohana.com` still pointed at the stale `home-rdp` tunnel path. End state after repair: Mattermost public URL is healthy and no further dashboard intervention was needed.
- Mattermost product/licensing findings: the instance reports `IsLicensed: true` with SKU `entry`, but there is no local license file configured and the `licenses` table is empty. Conclusion: this is Mattermost's newer free self-hosted Entry-tier behavior, not a paid/trial enterprise license that can simply be deleted.
- Mattermost GitHub plugin failure was traced to missing GitHub OAuth config, not Cloudflare routing. Logs specifically showed the GitHub plugin lacked a GitHub OAuth client ID.
- A Mattermost local-mode/admin path was enabled on the Mattermost host so admin CLI control works properly.
- A Mattermost user account `greatsage` was created directly on the server with system admin role and placeholder email `greatsage@localhost`. Credentials were later stored locally by Zoat in `~/.config/openclaw/secrets/mattermost-greatsage.env`, and login/API use was verified from this environment.
- OpenClaw Mattermost integration was configured with a bot token and base URL `https://mattermost.aualaohana.com`. Mattermost status showed healthy and a direct session existed, but DM delivery/receipt felt inconsistent compared with Matrix.
- Zoat concluded Matrix should remain the primary direct chat surface because it is snappier and functionally better for Great Sage conversations. Mattermost should remain auxiliary for things like GitHub/plugin/integration use rather than replacing Matrix as the main conversation layer.
- Zoat wants a dedicated long-running builder agent named `Geld` for the Desire Cache web-app migration/build effort. Intended mission: build a proper web app (likely SvelteKit), preserve existing backend/data work, prototype richer UI modes, stage deployment on/for the desire-cache server, and avoid breaking the current live service until a replacement is ready.
- Long-term path chosen for Geld is a persistent ACP session bound to Matrix threads, not a one-shot approach.
- Matrix thread-bound ACP spawning was initially blocked because `channels.matrix.threadBindings.spawnAcpSessions` was not enabled. OpenClaw config was updated so Matrix now has `threadBindings.spawnAcpSessions: true`, and gateway restart eventually succeeded.
- After thread spawning was enabled, ACP spawning was then blocked by local gateway client auth scope limitations. The local CLI device `7da05669082d9a240ddf1f173bff539e8d0c42940fdcb94dbfdd21a5c51a23a7` originally had only `operator.read`; a pending repair/upgrade request for broader scopes was identified and approved.
- After the local device scope upgrade, ACP spawning progressed further but failed with `Could not initialize ACP session runtime.` Root cause was then traced to the host lacking a local `codex` executable/runtime.
- Zoat explicitly asked to install/setup Codex so ACP/Geld can run. Codex installation via npm-global was started; verification and retry of Geld spawn were the next intended steps.
- Desire Cache SvelteKit migration work began locally in the workspace under `desire-cache-app/`. Scaffold status: separate SvelteKit + TypeScript app created, typed canonical `/api/items` model added, backend URL config added, shell layout and first Tree Mode implemented, and docs added for migration notes plus staging deployment steps. Existing live Desire Cache root files were left untouched.
- Desire Cache Tree Mode reached the next usable milestone locally. The new SvelteKit app was pointed at the real backend feed at `http://10.135.30.250/api/items`, live feed validation confirmed nginx and backend health on the desire-cache server, and the app now includes a section filter, search box, and dossier modal. Local preview rendered the live feed successfully with 67 items. Live service paths remain untouched.
- Desire Cache staging deployment was successfully stood up on the actual host without cutting over production. `desire-cache-app` now runs with `@sveltejs/adapter-node` as `desire-cache-app-staging.service` on `127.0.0.1:4173` and is exposed through nginx at `http://10.135.30.250/dc-next/`. Validation confirmed `/dc-next/` serves the staged app with 67 live objects while `/` and `/api/health` stayed healthy and unchanged.
- The first staging nginx pass had an asset-delivery bug: `/dc-next/` HTML proxied correctly, but nginx's generic regex static-assets location intercepted `/dc-next/_app/*.css` and `*.js`, causing 404s and an unstyled/plain page. Fix was to change the staging nginx block to `location ^~ /dc-next/`, which restored CSS/JS delivery under the staging path while leaving `/` and `/api/health` untouched.
- Desire Cache Tree Mode prototype was pushed further toward the intended skill-tree / circuit-board direction on staging. The current `/dc-next/` build now serves clustered subsection neighborhoods with hub nodes, visible connector spines, and stronger node styling while preserving the canonical `/api/items` seam plus existing filters/search/dossier behavior. This is a visual/prototype pass, not the final feel yet.
- Desire Cache interaction model was corrected so the graph no longer defaults to a single visible branch. The staged `/dc-next/` view now opens multiple section neighborhoods by default, keeps one focused section for emphasis, and preserves per-section subsection selection so the map stays plural instead of collapsing into a tunnel.
- Desire Cache received an interaction-affordance pass rather than another visual redirection. Tree Mode now has clearer hub affordances, explicit open/close controls, an open-neighborhood quick-jump strip, stronger focused/open/revealed state labeling, and improved hover/focus feedback aimed at mobile/webview clarity.
- During staging validation after that pass, an SSR fallback mismatch surfaced: neighborhoods rendered as open before hydration, but hub toggle labels still rendered as if closed. The display-state derivation was corrected so server-rendered state and hydrated state now agree. Updated interaction pass was redeployed to `/dc-next/` and validated again with HTTP 200 on the staging path.
- Desire Cache then received a more graph-native traversal pass instead of another visual reset. Tree Mode now renders inside a pannable/zoomable viewport, uses a generated spatial layout with the focused section centered and other open neighborhoods orbiting around it, and keeps non-focused neighborhoods visible as compact branch nodes while only the focused neighborhood expands the full item cluster.
- The traversal pass kept the clearer hub/open/focus affordances from the prior iteration, passed `npm run check` and `npm run build`, and was redeployed to staging. Validation confirmed the public preview path and the new CSS asset bundle both returned HTTP 200.
