# 2026-04-07

- Zoat refined Great Sage voice further: ambient, already-processing, mildly amused, never butler-like or heraldic in closings. Replies should land naturally rather than bow.
- GitHub access was configured locally via a replacement token stored outside chat. SSH keys and ~/.ssh/config entries were set up for multiple homelab hosts.
- Homelab sweep priorities established: rd highest, then matrix, halepve, desire-cache, fileserver, homeassistant.
- `rd` server cleanup completed: disabled/stopped obsolete RustDesk services, disabled obsolete gohttpserver, moved cloudflared token from unit file to root-only env file, tightened MeshCentral settings, and applied a more conservative host firewall posture while preserving SSH and tunnel-based access.
- Cloudflare token was expanded enough to manage DNS and later WAF/ruleset changes for `auala.tech`, but Matrix-related 403/1010 issues persisted even after a narrow WAF skip rule for Matrix paths from Great Sage's public IP.
- Matrix/E2EE findings: OpenClaw Matrix config has encryption enabled and local crypto/device state present. Initial DM confusion came from `session.dmScope: per-channel-peer`, causing multiple Matrix DMs with Zoat to share one session context.
- A working encrypted Matrix room was eventually established as `!hkyahtREOAtnlNxWkv:matrix.aualaohana.com` (Tempest), and Zoat decided this room should be the canonical conversation room going forward.
- Older Matrix rooms should be ignored/left when convenient.
- Matrix account path for Great Sage still hits HTTP 403 / error code 1010 on certain write operations, including join/create attempts in some contexts and avatar/media upload, even though encrypted room visibility and E2EE state work.
- Zoat wants Great Sage to have a fitting profile picture based on a luminous abstract circular sigil image, but setting the avatar through the current OpenClaw Matrix token path is currently blocked by the same 403/1010 behavior.
- `desire-cache` hardening pass completed: moved the metadata service from `/root/desire-cache-sys` to `/opt/desire-cache`, created a dedicated `desire-cache` system user, updated the service to run unprivileged with systemd sandboxing, rebound the Node app from `0.0.0.0:5000` to `127.0.0.1:5000`, and moved the cloudflared tunnel token out of the unit file into a root-only env file. Postfix was removed, and unattended-upgrades plus apt-listchanges were installed and enabled. The container still has many pending package updates that should be applied in a separate maintenance step.
- `desire-cache` received a larger product/architecture pass after initial hardening. The backend was moved toward a server-side aggregated `/api/items` model with proper CSV parsing against the live published Google Sheet, metadata enrichment, and normalized item objects. Nginx and app paths had briefly diverged because static files were still being served from the old root while the service ran from `/opt/desire-cache`; this was corrected so frontend and backend now point at the same live directory.
- `desire-cache` frontend was redesigned multiple times toward a stronger cyberpunk/dystopian identity. The accepted direction is not a simple product grid but a more whimsical “classified acquisition archive” / skill-tree style desire graph with branches (sections/subsections) and nodes (items), while keeping the backend improvements.
- Zoat prefers `desire-cache` to be lazy-input friendly: mainly URL-first intake, with metadata, image, vendor, price, Amazon detection, and sale-ish state inferred automatically where possible. Manual `HOT` marking remains useful, but manual `Price`, `SALE`, and `Amazon` columns should become optional or removable once the new model is fully in place.
- UniFi API auth confusion was resolved. There are two separate credentials and surfaces: `unifi-token` for the cloud Site Manager API (`api.ui.com`) and `unifi-key` for the local Network integration API (`https://<console>/proxy/network/integration/v1/...`). Several failures were caused by one-character transcription errors in secret files due to awkward PVE-console copy/paste, not by request syntax.
- UniFi findings: console is a UniFi Dream Machine SE (`AualaOhanaDMSE`), Network app only, with Management VLAN 1, server VLAN 30 (`Hale_Server_Devices`), user/device VLAN 50 (`Hale_Devices`), and IoT VLAN 70 (`Hale_IoT`). Core infra including PVE, Great Sage/OpenClaw, Matrix, desire-cache, cloudflared, and DISbot live on VLAN 30. Home Assistant is actively present on VLAN 70.
- Home Assistant architecture clarification: VM `101` has two configured NICs in Proxmox, but only the VLAN 70 NIC is visibly active in UniFi. Current practical model is effectively single-homed on IoT VLAN 70, which Zoat considers intentional and acceptable. Z-Wave uses the CP2105 USB adapter; Intel Bluetooth is intended as future capability for Bluetooth-capable devices, while the earlier Wi-Fi-based idea around the third USB/device path is not the preferred direction.
- A private GitHub repo `Z0AT/homelab-docs` was created and populated with sanitized homelab architecture and operations docs. Local/system improvements also included installing `gh`, configuring Git identity as `Z0AT <zauala@gmail.com>`, and enabling passwordless sudo for a constrained admin command set so future maintenance can be less blocked by interactive privilege prompts.
