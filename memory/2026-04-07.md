# 2026-04-07

- Zoat refined Great Sage voice further: ambient, already-processing, mildly amused, never butler-like or heraldic in closings. Replies should land naturally rather than bow.
- GitHub access was configured locally via a replacement token stored outside chat. SSH keys and ~/.ssh/config entries were set up for multiple homelab hosts.
- Homelab sweep priorities established: rd highest, then matrix, halepve, desire-cache, fileserver, homeassistant.
- `rd` server cleanup completed: disabled/stopped obsolete RustDesk services, disabled obsolete gohttpserver, moved cloudflared token from unit file to root-only env file, tightened MeshCentral settings, and applied a more conservative host firewall posture while preserving SSH and tunnel-based access.
- Cloudflare token was expanded enough to manage DNS and later WAF/ruleset changes for `auala.tech`, but Matrix-related 403/1010 issues persisted even after a narrow WAF skip rule for Matrix paths from Great Sage's public IP.
- Matrix/E2EE findings: OpenClaw Matrix config has encryption enabled and local crypto/device state present. Initial DM confusion came from `session.dmScope: per-channel-peer`, causing multiple Matrix DMs with Zoat to share one session context.
- A working encrypted Matrix room was eventually established as `!hkyahtREOAtnlNxWkv:matrix.aualaohana.com` (Tempest), and Zoat decided this room should be the canonical conversation room going forward.
- Older Matrix rooms should be ignored/left when convenient.
- Matrix account path for Great Sage still hits HTTP 403 / error code 1010 on certain write operations, including join/create attempts in some contexts and avatar/media upload, even though encrypted room visibility and E2EE state work.
- Zoat wants Great Sage to have a fitting profile picture based on a luminous abstract circular sigil image, but setting the avatar through the current OpenClaw Matrix token path is currently blocked by the same 403/1010 behavior.
- `desire-cache` hardening pass completed: moved the metadata service from `/root/desire-cache-sys` to `/opt/desire-cache`, created a dedicated `desire-cache` system user, updated the service to run unprivileged with systemd sandboxing, rebound the Node app from `0.0.0.0:5000` to `127.0.0.1:5000`, and moved the cloudflared tunnel token out of the unit file into a root-only env file. Postfix was removed, and unattended-upgrades plus apt-listchanges were installed and enabled. The container still has many pending package updates that should be applied in a separate maintenance step.
